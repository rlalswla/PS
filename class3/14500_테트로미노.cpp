#include <vector>
#include <string>
#include <iostream>

using namespace std;

int main (){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int rows, cols;
    cin >> rows >> cols;
    
    for(int i = 0; i < rows; i++){
        for(int j = 0 ; j < cols ; j++){
            
        }
    }

    return 0;
}